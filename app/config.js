var config = {
    ulule: {
        username:'', // your username
        key:'' // check http://www.ulule.com/settings/privacy/
        
    },
    slack: {
        token:'', // check https://api.slack.com/docs/oauth-test-tokens
        channel:'' // the channel to push messages to
    }
    
};

module.exports = config;